{
  "name": "terrain2drenderer",
  "version": "0.9.0",
  "scripts": {
    "start": "npx webpack --watch",
    "build": "npx webpack",
    "test": "npx jest",

    "render": "cd .github/scripts/render && npm run geojson",
    "render:island": "cd .github/scripts/render && npm run island",
    "render:debug": "cd .github/scripts/render && npm run debug"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.4.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "canvas": "^2.11.2"
  }
}
